# Windows 安装包验证：v2026.08.21

## 本地验证

- 自动回归：84 项通过。
- 构建：PyInstaller 6.21.0 清洁构建通过。
- 安装器：Inno Setup 7.0.2 x64 编译通过。
- 安装：使用独立临时目录静默安装通过。
- 运行时：随包 `ffmpeg.exe`、`ffprobe.exe`、Node.js 与 YouTube PO Provider 均存在；FFmpeg 与 FFprobe 的 `-version` 可执行。
- 启动：安装后的 `南枫下载.exe` 隐藏启动并持续运行超过 8 秒。
- 卸载：静默卸载器成功运行；临时目录仅保留预先创建的空根目录，已作为测试临时目录处理。

## 发布前边界

- 本文只记录本地构建与安装级验证；GitHub 资产的大小和 SHA-256 需在上传完成后再核对。
- 外部平台 CDN 的瞬时降速或连接重置仍可能发生；本版将其显示为续传状态并按现有重试策略处理，不承诺固定下载速度。
